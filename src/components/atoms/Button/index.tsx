import React from 'react'

const index = (title: string) => {
  return (
    <button className='bg-red-500 w-[154px] h-[51px] px-8 py-4 rounded-tl-[5px]'>{title}</button>
  );
}

export default index;